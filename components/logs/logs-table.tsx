"use client";
import { useRef, useState } from "react";
import { X } from "lucide-react";
import type { MonitoringEvent } from "@/lib/types";
import { formatDate } from "@/lib/utils";
import { EmptyState } from "@/components/ui/empty-state";

export function LogsTable({ events }: { events: MonitoringEvent[] }) {
  const [selected, setSelected] = useState<MonitoringEvent>(); const dialog = useRef<HTMLDialogElement>(null);
  const open = (event: MonitoringEvent) => { setSelected(event); requestAnimationFrame(() => dialog.current?.showModal()); };
  if (!events.length) return <EmptyState title="No matching logs" description="Adjust the current search or filters to see request events." />;
  return <><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs text-[var(--muted)]"><tr>{["Method","Endpoint","Status","Duration","Project","Timestamp"].map((label) => <th key={label} className="px-4 py-3 font-medium">{label}</th>)}</tr></thead><tbody>{events.map((event) => <tr key={event.id} tabIndex={0} onClick={() => open(event)} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") open(event); }} className="cursor-pointer border-t hover:bg-[var(--surface-raised)]"><td className="px-4 py-3 font-mono text-xs">{event.method}</td><td className="max-w-80 truncate px-4 py-3 font-mono text-xs">{event.path}</td><td className="px-4 py-3 font-mono text-xs">{event.status_code}</td><td className="px-4 py-3 font-mono text-xs">{event.response_time}ms</td><td className="px-4 py-3">{event.project?.name ?? "Unknown"}</td><td className="px-4 py-3 text-xs text-[var(--muted)]">{formatDate(event.occurred_at)}</td></tr>)}</tbody></table></div><dialog ref={dialog} onClose={() => setSelected(undefined)} className="m-0 ml-auto h-full max-h-none w-full max-w-lg border-l bg-[var(--surface)] p-0 text-[var(--foreground)] backdrop:bg-black/60"><div className="flex items-center justify-between border-b p-5"><h2 className="font-semibold">Request details</h2><button onClick={() => dialog.current?.close()} aria-label="Close details"><X className="h-5 w-5" /></button></div>{selected ? <dl className="grid grid-cols-2 gap-5 p-5 text-sm">{[["Request ID",selected.id],["Method",selected.method],["Endpoint",selected.path],["Status",selected.status_code],["Response time",`${selected.response_time}ms`],["Project",selected.project?.name ?? "Unknown"],["Timestamp",formatDate(selected.occurred_at)],["Region",selected.region ?? "Not provided"],["User agent",selected.user_agent ?? "Not provided"]].map(([label,value]) => <div key={label} className={label === "Endpoint" || label === "User agent" || label === "Request ID" ? "col-span-2" : ""}><dt className="text-xs text-[var(--muted)]">{label}</dt><dd className="mt-1 break-words font-mono text-xs">{value}</dd></div>)}<div className="col-span-2"><dt className="text-xs text-[var(--muted)]">Metadata</dt><dd><pre className="mt-1 overflow-auto rounded-md bg-[var(--background)] p-3 text-xs">{JSON.stringify(selected.metadata, null, 2)}</pre></dd></div></dl> : null}</dialog></>;
}
